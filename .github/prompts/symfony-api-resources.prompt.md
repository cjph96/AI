---
description: Create or change API Platform resources in a Symfony project.
---

Use `.github/skills/symfony-api-platform-resources/SKILL.md` exactly as written for: ${input:goal}.

Prefer `symfony-implementer` for resource changes and `symfony-code-reviewer` for review-only requests.
