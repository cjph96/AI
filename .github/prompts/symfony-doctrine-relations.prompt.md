---
description: Design or review Doctrine relationships in a Symfony project.
---

Use `.github/skills/symfony-doctrine-relations/SKILL.md` exactly as written for: ${input:goal}.

Prefer `symfony-research-planner` for design and `symfony-implementer` for approved changes.
