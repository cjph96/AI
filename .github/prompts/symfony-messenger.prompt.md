---
description: Implement or review Symfony Messenger messages and handlers.
---

Use `.github/skills/symfony-messenger/SKILL.md` exactly as written for: ${input:goal}.

Prefer `symfony-implementer` for handler changes and `symfony-code-reviewer` for read-only review.
