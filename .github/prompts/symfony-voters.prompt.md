---
description: Implement or review Symfony voters and authorization flows.
---

Use `.github/skills/symfony-voters/SKILL.md` exactly as written for: ${input:goal}.

Prefer `symfony-implementer` for changes and `symfony-code-reviewer` for read-only review.
