---
description: Plan or implement Symfony functional tests for HTTP behavior.
---

Use `.github/skills/symfony-functional-tests/SKILL.md` exactly as written for: ${input:goal}.

If the task needs code changes, prefer `symfony-implementer`. If the task is read-only planning, prefer `symfony-research-planner`.
