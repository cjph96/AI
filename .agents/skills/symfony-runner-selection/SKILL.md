---
name: symfony-runner-selection
description: >
  Selects the correct runner for Symfony commands in host, Docker Compose, Symfony Docker, or DDEV environments.
  Use when commands, tests, or console tasks must run in a Symfony repo.
  Do not use for pure code review or non-Symfony projects.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/symfony-runner-selection/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/symfony-runner-selection/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/symfony-runner-selection/` as directed.
4. Do not invent behavior outside the canonical skill.