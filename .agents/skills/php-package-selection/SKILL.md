---
name: php-package-selection
description: >
  Selects PHP dependencies with a Composer-first and framework-aware decision process.
  Use when a PHP change needs a new package or when an existing dependency should be replaced.
  Do not use when the package is already mandated or when the task is pure implementation with no dependency choice.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/php-package-selection/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/php-package-selection/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/php-package-selection/` as directed.
4. Do not invent behavior outside the canonical skill.