---
name: javascript-package-selection
description: >
  Selects JavaScript and TypeScript dependencies with a framework-first, runtime-aware decision process.
  Use when a JS/TS task needs a new package, state library, schema validator, test tool, or frontend platform choice.
  Do not use when the dependency is already mandated or when the task is pure implementation with no selection tradeoff.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/javascript-package-selection/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/javascript-package-selection/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/javascript-package-selection/` as directed.
4. Do not invent behavior outside the canonical skill.