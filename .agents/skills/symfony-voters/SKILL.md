---
name: symfony-voters
description: >
  Implements and validates Symfony voters and resource authorization paths.
  Use when access depends on the current actor and domain object state.
  Do not use for simple role checks that do not need object-level authorization.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/symfony-voters/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/symfony-voters/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/symfony-voters/` as directed.
4. Do not invent behavior outside the canonical skill.