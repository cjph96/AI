---
name: laravel-package-selection
description: >
  Selects Laravel-first solutions and only escalates to external packages when the built-in framework surface is insufficient.
  Use when a Laravel task needs a package or cross-cutting capability.
  Do not use for plain PHP or Symfony repositories, or when an exact package was already chosen.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/laravel-package-selection/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/laravel-package-selection/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/laravel-package-selection/` as directed.
4. Do not invent behavior outside the canonical skill.