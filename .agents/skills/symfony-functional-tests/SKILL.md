---
name: symfony-functional-tests
description: >
  Designs and implements Symfony functional tests with WebTestCase for controllers, HTTP endpoints, API Platform operations, and security flows.
  Use when user-visible HTTP behavior changes.
  Do not use for pure domain logic or config-only changes.
---

This is a thin Codex compatibility wrapper for the canonical skill at `.github/skills/symfony-functional-tests/SKILL.md`.

When this skill is invoked:

1. Read `.github/skills/symfony-functional-tests/SKILL.md`.
2. Follow the canonical instructions there exactly.
3. Read any referenced files under `.github/skills/symfony-functional-tests/` as directed.
4. Do not invent behavior outside the canonical skill.